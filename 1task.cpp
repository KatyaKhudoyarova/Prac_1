#include <iostream>
#include <string>
#include <cmath>
#include <math.h>

int main() 
{
    int a=3;
    double b=0.523;
    double result;
    result = (pow(sqrt(1.56*sqrt(sin(b))) / (0.8942*log(a)),0.25));

  std:: cout<< result;//std:cout - виведення на консоль.
  
}